{
  "name": "proyecto-surtitelas",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev:full",
    "start": "cd backend && npm run start",
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../backend && npx prisma generate"
  }
}
